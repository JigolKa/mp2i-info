#include <stdio.h>
#include <stdlib.h>
#include <assert.h>
#include <stdbool.h>

#include "file_tab.h"
#include "elem.h"


fileT file_vide(int cap){
	assert(false);
}


void libere_file(fileT f){
	assert(false);
}

void affiche_file(fileT f){
	assert(false);
}


bool est_file_non_vide(fileT f){
	assert(false);
}

type_elem tete(fileT f){
	assert(false);
}


void enfile(fileT* f, type_elem e){
	assert(false);
}


void defile(fileT* f){
	assert(false);
}
